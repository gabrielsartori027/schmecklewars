# data/

`aid-drops.json` — registry of Aid Drops (Phase 1). Each entry carries `legs[]` (Safe → bridge →
destination chain → organization) and is shown as `Verified` only when every leg is confirmed on-chain.
Empty at launch on purpose: no drop exists before Epoch 1 closes.
